# llm-notes-and-scripts
Notes: nothing here is polished and production ready.
These are learning artifacts, scripts I rebuilt from scratch with comments that reflect my understanding.

## Scripts
- `load_model_and_generate.py`: load a model and its tokenizer.
  run a prompt through the pipeline: tokenize -> generate -> decode.
  Uses Qwen3-0.6B

## Setup
Python 3 
```
pip install transformers torch
```
